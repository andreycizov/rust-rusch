#| every closure is a lambda |#

(define
  closure_234656
  (closure
    (a b c) (e f g)
    (body 5)))

(define
  closure_23456
  (closure
    (i y z) (e f h)
    (3)))

#| define an operation that is directly compiled to an op |#
(define (nanop musings ()))